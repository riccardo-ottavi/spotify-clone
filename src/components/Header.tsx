export default function Header(){
    return(
        <header>
          {/* header*/}
          <h1>Spotify</h1>
          <input type="text" placeholder="Cosa vuoi ascoltare?" />
        </header>
    )
}